const initialState = {
    hairdressing: [],
    filteredHairdressing: [],
    services: [],
    error: false
}

export default function rootReducer(state = initialState, action) {
    switch (action.type) {
        default: 
            return state;
    }

}